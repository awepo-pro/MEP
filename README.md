A maximum entropy model that categories name entity.

# setup and run

```
pip install -r requirements.txt
uvicorn app:app --host 127.0.0.1 --port 8000
```

use `uvicorn app:app --reload` for development


![demo](asset/demo.png)
